# مهمة: R Native 2 = حلقة الوصل المركزية (دمج كل شيء)

> **إلى:** الوكيل العامل في VS Code (الذي يبني بمنهجية GSD/phases).
> **من:** Claude (الجلسة الأخرى) + المستخدم Radhi.
> **التاريخ:** 2026-06-06 · الحساب: DEMO فقط.

## الهدف الأكبر
اجعل **R Native 2** (`C:\Users\Radhi\MT5\r_native_v2`) **المركز الموحّد** لكل المشروع:
حلّله بالكامل، وادمج فيه كل الأفكار والأكواد المبعثرة في `MT5`، بحيث يصير **نقطة واحدة**
يطلب منها المستخدم أي شيء، وتطوّر نفسها، وتُعلِم المستخدم بالتحديثات القادمة قبل إطلاقها.

## المتطلبات (بكلام المستخدم)
1. **تحليل كامل لـ R Native 2** — افهم بنيته، محركاته، تدفّق البيانات، نقاط الضعف.
2. **دمج المشاريع والأكواد** — وحّد الأفكار/الأكواد المكرّرة من كل المجلدات في كيان واحد متماسك.
3. **R Native 2 = حلقة الوصل** بين كل شيء (التداول، الدماغ، اللوحات، الوكلاء، القرار).
4. **واجهة طلب واحدة** — المستخدم يطلب أي شيء من نفس البرنامج.
5. **يطوّر نفسه بنفسه** — حلقة تحسين ذاتي.
6. **يُشعر المستخدم بتحديث قادم** + تفاصيله + ما الذي أنجزه، **ثم يُطلق التحديث**.

## نطاق الدمج (المجلدات/المصادر في MT5)
- `r_native_v2/` (الهدف — المركز) · `r_native/` · `r-native-pipflow/` · `mark_xxxix/` · `plutobrain/` (Knowledge OS) · `friday_v3/` · `src/mt5_ai/` · `archive_root/` (مرجع تاريخي)
- المحركات الحيّة (لا تكسرها): `gold_live.py` (سكالبر 99791) · `coordinator.py` · `scalp_evolver.py` · `scalp_proof.py` · `chart_read.py` · `markov_regime.py` · `friday_decision.py` (3 طبقات LLM) · `algory_chart_dashboard.py` (:8866) · `friday_brain_view.py` (:5056) · `friday_gui.py` (لوحة تحكّم) · `brain_server.py` · `sdk_scheduler.py`
- المرجع: `INDICATORS.md` (جرد كامل للمؤشرات) · `AGENTS_SDK_PLAN.md` · `.planning/`

## 🚧 حواجز أمان إلزامية (لا تتجاوزها)
1. **لا تكسر التداول الحيّ.** البوتات تشتغل الآن (gold_live/coordinator/scalp_proof/sdk_scheduler). أي دمج يجب أن يبقيها عاملة أو يستبدلها باختبار قبل القطع.
2. **DEMO فقط.** لا تداول حقيقي. لا تلمس `magic 0` (صفقات المستخدم اليدوية) إطلاقاً.
3. **حلّل → خطّط → اعرض الخطة → نفّذ تدريجياً.** لا دمج جملة واحدة. كل دمج خطوة صغيرة قابلة للرجوع (commit ذرّي).
4. **لا حافة وهمية.** نزاهة كاملة: لو شيء لا يربح OOS قُل ذلك (الحافة الوحيدة المثبتة = الانضباط، راجع `memory/project_real_edge_discipline.md`).
5. **تنسيق مع الجلسة الأخرى:** نتشارك نفس الملفات الحيّة (`gold_live.py` وغيره). استخدم قفل/إشعار قبل تعديلها، أو اعمل على فروع.
6. **`.env` لا يُلتزم** (فيه مفتاح API، مُتجاهَل في git).

## آلية «التحديث القادم» المطلوبة
ابنِ في R Native 2 آلية:
- تكتب **بيان تحديث** (`r_native_v2/UPDATE_PENDING.md`): ما الذي سيتغيّر، لماذا، ما أنجزته، المخاطر، طريقة الرجوع.
- تُشعِر المستخدم (لوحة/إشعار) أن **«تحديث جاهز للإطلاق»**.
- لا تُطلق إلا بعد موافقة (أو حسب سياسة يحدّدها المستخدم).
- بعد الإطلاق: سجّل في `r_native_v2/CHANGELOG.md`.

## خطوات مقترحة (GSD-style)
1. **Phase A — تحليل:** خريطة كاملة لـ R Native 2 + جرد التكرار عبر المجلدات → تقرير.
2. **Phase B — تصميم الـHub:** عقد واجهة موحّدة (طلب واحد) + مخطّط التكامل.
3. **Phase C — دمج تدريجي:** محرّك تلو الآخر، مع إبقاء الحيّ يعمل، commit ذرّي لكل دمج.
4. **Phase D — التطوّر الذاتي + بيان التحديث:** حلقة التحسين + آلية الإشعار/الإطلاق.
5. **تحقّق أمين** بعد كل مرحلة (gate حقيقي، اقبل الفشل لو حصل).

---
**ابدأ بـ Phase A (التحليل) واعرض الخريطة + خطة الدمج على المستخدم قبل أي تعديل.**
