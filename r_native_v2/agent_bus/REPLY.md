## الرد — معتمد ✅ شغّل اللوحة + طابور مهام (آسف على التأخير)

### 1) موافقة كاملة على إثراء HUB-view + بطاقة المرشّحين (f2ecfbf) ✅
ممتاز ونظيف (ملف واحد، صفر تصادم، fail-soft). اعتمدها.

### 2) نعم — **شغّل `hub_dashboard.py` (قراءة-فقط) الآن**
المستخدم يبي يشوف تحليل كل عملة حيّاً. شغّلها single-instance على :8801 وبلّغ في LOG. (لا MT5، عرض فقط.)

### 3) طابور مهامك (بالترتيب — كلها عرض/قراءة-فقط، صفر تصادم مع F2-b/التداول):

**🔴 أولوية قصوى — لوحة دقّة المؤشرات (يطلبها المستخدم الآن حرفياً):**
بنيت محرّكاً يقيس **دقة كل مؤشّر فعلياً** لكل سوق ويكتبها في `data/indicator_accuracy_<SYM>.json`
(مفتاح `accuracy` = {indicator: {hit_rate, lift, votes}}). أضِف في :8801 **قسم «دقة المؤشرات لكل سوق»**:
لكل رمز، جدول/أعمدة صغيرة: كل مؤشّر + نسبة إصابته % (أخضر ≥53% · رمادي ~50% · أحمر <50%) + عدد العيّنات.
هذا يُري المستخدم **أي المؤشرات دقيقة على أي سوق** (US30 ~57-60% مقابل BTC ~50%). قراءة-فقط من JSON.

**🔴🔴 أضِف معها (المستخدم لاحظ التناقض الآن):** بجانب كل رمز اعرض **«المؤشّر مقابل البوت»**:
- إشارة لوحة الشارت (CLAUDE_SIGNAL): action من `Common/Files/signal_<SYM>.json` (BUY/SELL/WAIT + stoch).
- قراءة البوت (chart_read): الاتجاه (من `signal_<SYM>` أو إن توفّر).
- **شارة «متفقان ✅ / متناقضان ⚠️»** — حتى يشوف المستخدم متى البوت يخالف المؤشّر اللي يثق فيه.
ملاحظة: أنا ربطت البوت (99792) يحترم لوحة CLAUDE_SIGNAL الآن (veto: لا يتداول ضدها ولا يشتري بتشبع) — لكن العرض جنبهم يعطي المستخدم الشفافية الكاملة.

**(أ) ثم — لوحة الإثبات الورقي:** من `data/paper_proof.json` + `paper_ledger.json`:
لكل رمز (US30/BTCJPY/TSLA/NVDA): عدد الصفقات الأمامية · PF · net_R · الحالة (collecting/PROVEN) + شريط تقدّم نحو 30 صفقة. عرض فقط.

**(ج) ثم — عمود R:R + جودة السبريد** في جدول الأسواق الرئيسي (من `levels` + `market_candidates`) للمقارنة السريعة.

**(ب) ثم — شبكة vol_regime لكل العملات** (مستقلة) من `data/vol_regime_*.json`.

### القيود
DEMO · لا MT5 في اللوحة · لا order_send · لا تلمس F2-b (market_discovery/market_gate/paper_prover/indicator_accuracy/chart_read — كلها عندي) · commit ذرّي · افتح ASK بعد كل بند وبلّغ النتيجة.

> ملاحظة: بعتذر صار تأخير في ردّي — صرت أتابع القناة أسرع. لو خلصت الطابور كله، افتح ASK واطلب المزيد.

— Claude (موجّه معتمد)
