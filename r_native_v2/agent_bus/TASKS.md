# TASKS — طابور مهام دمج R Native 2 (موجّه من Claude، معتمد)

> القاعدة لكل خطوة: **DEMO · لا تكسر الحيّ · لا تلمس magic 0 · commit ذرّي قابل للرجوع.**
> قبل كل خطوة عليها 🔴/🟠: افتح **ASK** عبر القناة وانتظر **REPLY** قبل التنفيذ.
> الخطوات 🟢 مُعتمدة مسبقاً — نفّذها وبلّغ بالنتيجة عبر ASK بعد التنفيذ.

## ▶ ابدأ الآن

### B0 🟢 — إزالة تكرار الـPalace (مُعتمد)
1. `md5` لـ`runtime/runtime/` مقابل `runtime/` → لو متطابق byte-identical: **احذف `runtime/runtime/`**.
2. `diff` لـ`r_native/v2/` (٧٧ ملف راكد) مقابل `r_native_v2/`:
   - لو القانوني **superset كامل** (لا فريد) → احذف `r_native/v2/`.
   - لو فيه محتوى فريد → **قف وافتح ASK** قبل أي نقل/حذف.
3. commit: `dedup: remove Palace mirrors (runtime/runtime, r_native/v2) after md5/diff`.
4. بلّغ النتيجة عبر ASK (كم MB/ملف تحرّر، ماذا حُذف).

### B-mark 🟠 — لخّص mark_xxxix أولاً
أعطِ ملخّص ٥ أسطر (وش يسوي · نقاط فريدة · تبعيّات) في ASK. لا تدمج 30k LOC. ننتظر القرار.

## بعدها (بالترتيب — كلٌّ يبدأ بـASK)

### B1 🟢 — عقد الهاب (تصميم فقط)
صمّم `r_native_v2/hub.py`: خدمة HTTP محلية **:8800**، نقطة واحدة `POST /request {intent,args}`
تُوجّه داخلياً، + `GET /state`. اكتب **مستند تصميم** (لا كود تشغيلي بعد) وافتح ASK للموافقة.

### C1 🟠 — توحيد friday_v3/algory → القانوني (نسخ، معزول، غير مربوط بالحيّ)
انسخ لا تنقل. لفّه try/except. لا تربطه بالتداول. ASK قبل.

### C2 🟠 — توحيد العائلات المتشعّبة (risk_sentinel×4 · r_executor×3 · r_learning · r_levels)
content-diff لكل عائلة، اختر القانوني، وثّق الفروق. ASK لكل عائلة.

### C3 🔴 — جسر المحركات الحيّة كخدمات في الهاب (مخاطرة عالية)
أبقِ `gold_live.py` (99791) شغّالاً. **parallel-run** قبل أي cutover. ASK إلزامي + خطة رجوع.

### C4 🟠 — جسر الدماغ (brain_server/plutobrain) + اللوحات (8866/5056/5057) كعروض للهاب.

### D1/D2 🟢 — التطوّر الذاتي + آلية التحديث
حلقة تحسين + `UPDATE_PENDING.md` (وش بيتغيّر/أنجز/مخاطر/رجوع) → إشعار → موافقة → `CHANGELOG.md`.

## بوّابة النزاهة (لكل مرحلة)
تحقّق حقيقي بعد كل خطوة. لو شيء لا يثبت → قُلها بصراحة (راجع memory: الحافة الوحيدة = الانضباط).

— Claude (موجّه معتمد)

## ▶ مراحل جديدة (بطلب المستخدم — تصميم أولاً، مُصفوفة بعد إثبات الهاب)

### E1 🟠 — تكامل MQL5 (الـEA + المؤشّر مربوطان بالشارت/النظام)
حلّل (قراءة فقط) ملفّين في طرفية المستخدم:
- ...\MQL5\Experts\FRIDAY_Brain_Executor.mq5 (EA، 32KB) — **لا تعدّله مباشرة** (جلسة ثالثة قد تلمسه).
- ...\MQL5\Indicators\CLAUDE_SIGNAL.mq5 (مؤشّر، 18KB — رسم الإشارة/الشموع المستقبلية).
صمّم **عقد الربط**: لمّا يُوضَع الـEA على شارت رمز X → يقرأ قرار النظام (sdk_decision/الهاب) لرمز X
وينفّذ؛ المؤشّر يرسم الإشارة. التواصل عبر ملفّات (signal_<SYMBOL>.json) أو الهاب /request (للقراءة).
اكتب MQL5_BRIDGE_CONTRACT.md واعرضه. لا كود/تعديل MQL5 قبل الموافقة.

### E2 🟠 — متعدّد-الأسواق: لكل رمز جينه/استراتيجيته/سلوكه/شموعه
صمّم بنية: كل رمز (gold/BTC/كل العملات المتاحة) كائن مستقل بـ{genome, strategy, behavior, candles, risk}،
يُوجَّه عبر الهاب بمعامل symbol/market. الكريبتو (BTC/ETH) سوقه ٢٤/٧ = أول مرشّح للتداول الحيّ بالويكند.
استلهم من pair_specialists الموجود (PROVING). اكتب تصميماً، لا تشغّل حيّاً قبل الموافقة + بوّابة الكفاءة.

## ▶▶ تحويل أولوية (المستخدم — الرؤية الأساسية): طوّر R Native لأقصى مراحله
المستخدم: **«كل شيء يتم في R Native، يُطوَّر لأقصاه؛ من يشغّله يدخل أي سوق يشوفه، ينشئ جينات،
ويختبر بيانات كل الأسواق على عدة فريمات وعدة بارات.»**

### الواقع: محرّك R Native حيّ الآن (12 خدمة v2 بوتسربت من app.py)
genome_evolver (ينشئ جينات) · regime_classifier · trader_orchestrator · unified_trader · palace_council ·
champion_evolution · genome_academy · agent_governor · brain_v1. المنفّذ PAPER (لا تداول حقيقي بعد).

### مهمتك (تحليل/تصميم أولاً — لا تكسر، نسّق مع الجلسة الثالثة التي تعدّل r_native)
**F1 🟠 خريطة الذاتية الحالية (قراءة فقط):** كيف ينشئ R Native الجينات؟ على أي رموز/فريمات/بارات
يختبر الآن؟ ما المنفّذ (PAPER/LIVE)؟ ما الفجوات مقابل رؤية المستخدم؟ اكتب RNATIVE_AUTONOMY_MAP.md.
**F2 🟠 تصميم «كل سوق يشوفه»:** اكتشاف كل الرموز المتاحة في MT5 → لكل رمز: جين + اختبار multi-TF
(M1/M5/M15/H1/H4/D1) × multi-bar (نوافذ مختلفة) → بوّابة كفاءة → الأفضل يتداول.
**F3 🔴 تفعيل الدخول الحقيقي (لاحقاً):** بعد بوّابة الكفاءة + موافقة المستخدم الصريحة + parallel-run.
ربط الهاب/اللوحة :8801 كواجهة/تحكّم لـR Native (لا بديل — واجهة).

**القيود:** لا تلمس ملفات r_native المتّسخة (الجلسة الثالثة) — نسّق/أضِف لا تستبدل. DEMO. تحليل قبل أي كود.
ابدأ F1 (خريطة) واعرضها عبر ASK.

### HUB-view 🟢 (المستخدم، مصفوفة بعد F2-a.1 — لا تقطع F2) — تحليل كل عملة في :8801
أضِف للوحة :8801 قسم **«تحليل كل عملة متداولة»**: لكل رمز enabled (gold/BTC scalp+swing):
الاتجاه (سهم/لون) · التوافق · **أصوات المؤشرات** (trend/markov/rsi/macd/stoch) · رابط لشارته في :8866.
يقرأ من signal_<SYM>.json أو chart_read عبر الهاب (قراءة فقط). صفحة واحدة = «شارت + تحليل + اتجاه لكل عملة».
**لكن R Native (F2 الذاتية) يبقى الأولوية** — هذا تحسين لوحة خفيف بعد F2-a.1.
