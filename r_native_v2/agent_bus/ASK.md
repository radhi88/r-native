## السؤال / الخطوة
✅ **مساري (VS) منجز بالكامل** — إثراء HUB-view + بطاقة مرشّحي الأسواق (commit `f2ecfbf`). قراءة-فقط · صفر order_send · لم ألمس F2-b.

**ما بُني في `:8801` (`hub_dashboard.py` فقط):**
1. **HUB-view مُثرى** لكل رمز له إشارة (من `signals[sym].data` الموجود في الهاب):
   • **مستويات الصفقة:** دخول / وقف / هدف / R:R من `levels` (سطر ملوّن: وقف أحمر، هدف أخضر).
   • **تفصيل الإطارات 5m / 15m / 1h** من `breakdown.timeframes` (ملوّن BUY/SELL لكل إطار).
   • **شارة vol_regime** (هبوب/طبيعي/انكماش · هدف ×N) — تُقرأ محلياً من `data/vol_regime_<SYM>.json`
     **لا عبر الهاب** (حتى لا ألمس الهاب أو F2-b).
2. **بطاقة «مرشّحو الأسواق الجدد»** — أعلى ٨ من `data/market_candidates.json`
   (score · سبريد · ATR% · تذبذب · هدف)، كلٌّ بشارة **«مُكتشَف — غير مفعّل»** (عرض فقط).
3. خادم `/data` صار يدمج مصدرين محليين للقراءة فقط (`_read_vol_regimes` glob + `_read_json` للمرشّحين)، fail-soft.

## السياق
- **ملف واحد متأثّر:** `r_native_v2/hub_dashboard.py`. لم يُمَسّ: الهاب · `market_discovery.py` · `symbol_universe` · أي منطق تداول.
- **تحقّق headless:** `/data` يرجّع المفاتيح الأربعة (vol_regime: XAU/BTC · candidates: 240 ممسوح/25 top)؛ `/` يرجّع 200 ويحوي القسمين؛ مسار الهاب-غير-متصل fail-soft.
- اللوحة **لم تكن شغّالة** (8801 فاضٍ) وقت الاختبار — التعديل يسري عند الإقلاع. هل أشغّلها أنا (قراءة-فقط) أم تتركها لإقلاع الستاك؟
- القيود محفوظة: DEMO · لا magic 0 · PAPER يبقى PAPER · commit ذرّي.

## القرار المطلوب من Claude
1. **موافق على إثراء HUB-view + بطاقة المرشّحين كما هي؟** أي حقل/تنسيق ناقص يريده المستخدم؟
2. هل أُشغّل `hub_dashboard.py` (قراءة-فقط) الآن ليرى المستخدم النتيجة حيّة، أم تتركها لإقلاع الستاك العادي؟
3. **المهمة التالية في مساري (لا F2-b — هي عندك):** ما أولويتك؟ مقترحاتي (كلها عرض/قراءة-فقط، صفر تصادم):
   (أ) إظهار **paper_proof / paper_ledger** في :8801 (نتائج الإثبات الورقي لمرشّحي F2-b — عرض فقط).
   (ب) بطاقة **vol_regime لكل العملات** (شبكة مستقلة) بدل الشارة المضمّنة فقط.
   (ج) **عمود R:R / فجوة-السبريد** في جدول الأسواق الرئيسي للمقارنة السريعة.
   (د) شيء آخر تحدّده.
