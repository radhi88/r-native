# FRIDAY / R-Native — الخطة الكاملة وسير العمل (2026-06-11)

> الهدف النهائي: نظام تداول ذاتي التطوّر يثبت **ربحية حيّة مستدامة** على DEMO ثم — بإذن صريح — فلوس حقيقية.
> المبدأ الحاكم: **لا حافة بلا إثبات OOS بسبريد حقيقي، ولا توسّع بلا إثبات حيّ، واللوحة الصادقة هي الحكم.**

---

## 1) سير العمل الكامل (الطبقات والترابط)

```
┌─────────────────────────── طبقة المصادر (Inputs) ───────────────────────────┐
│ MT5 (أسعار/صفقات)                                                            │
│ news_engine (60ث): 5 RSS + رزنامة FF ──► news_signals.json (لحظي+EWMA+فيتو) │
│   └ طبقات وزن: كلود×5 (news_curated) ► رسمي×3 ► RSS×1.5                      │
│ intermarket (45ث): مصفوفة ارتباط حيّة ──► intermarket_signals.json           │
│ chart_read: ~31 مؤشر تصويت موحّد (أوزان متعلّمة لكل عملة)                    │
│ vol_regime: تمدّد/انكماش المدى ──► تحجيم الهدف فقط                           │
└──────────────────────────────────────┬───────────────────────────────────────┘
                                       ▼
┌────────────────────── طبقة التعلّم والتحقّق (Learning) ──────────────────────┐
│ market_discovery (238 رمز) ► market_gate (OOS بلا تسريب، 4 نوافذ)            │
│ ► genome_factory: شبكة+تهجين جيني · 50k شمعة · سبريد حقيقي لكل شمعة          │
│   · حارس الأفضل-فقط · بوّابة سيولة (سبريد≤40% ATR)                           │
│ ► جين عام لكل عملة + مجموعة متخصّصي جلسات (ASIAN/LONDON/NY×2)                │
│ ► deploy_genomes ► symbol_configs                                            │
│ حلقات مستمرة: accuracy_updater (10د، أوزان المؤشرات) · secure_learner (1س)   │
│ · _reoptimize_loop (3س، الخاسر أولاً+تطوّر جيني) · paper_prover (إثبات أمامي)│
│ · evolution_director (30د): تطوّر الأوضاع بالنتائج الحيّة + تحقّق يومي        │
│   للجلسات + مسبار اكتشاف يومي + مستشار الصيّاد                                │
└──────────────────────────────────────┬───────────────────────────────────────┘
                                       ▼
┌──────────────────────────── طبقة القرار (Decision) ──────────────────────────┐
│ llm_analyst (90ث): جودة الجين+أخبار+ترابط+ماكرو ► market_directive.json      │
│   (TRADE / TRADE_LIGHT / WAIT / AVOID) — المنفّذون يطيعونه                    │
│ risk_manager (60ث): دورة ICA كاملة (10 مخاطر، احتمال×أثر) ► risk_register     │
│   ► lot_mult (HIGH×0.7 / CRITICAL×0.4) + تعطيل التكديس                        │
│ mode_weights (30د): أوضاع الدخول تُحاسَب من أرباحها الحيّة ► تعطيل/تعزيز     │
└──────────────────────────────────────┬───────────────────────────────────────┘
                                       ▼
┌──────────────────────────── طبقة التنفيذ (Execution) ────────────────────────┐
│ multi_trader (20260608): موجّه الجلسات ► متخصّص الجلسة الحالية فقط           │
│   أوضاع: macro/xasset/dive/wick_rev/gap_pend/srlim — كلها خاضعة لـ           │
│   directive+risk+mode_weights+فيتو الأخبار · تكديس متزامن (مقيّد بالمخاطر)   │
│   إدارة: BE/تريل متعلّم · خروج الذيل · بنح النازف (−$8)                       │
│ straddle_hunter (20260611): كمّاشة الأخبار OCO+تبديل+تقييم ذاتي+طلب مساعدة   │
│ unified_trader (99782): ذهب FVG معلّقات + تريل كل المراكز + حماية العاري 12%  │
│ btc_live · coordinator · r_executor · brain_server+31 وكيل (lot_mult/حظر)     │
└──────────────────────────────────────┬───────────────────────────────────────┘
                                       ▼
┌───────────────────────────── طبقة الأمان (Safety) ───────────────────────────┐
│ قتل يومي 15% · بنح حيّ −$8 · حماية بلا-وقف (12% سقف) · بوّابة سيولة          │
│ فيتو أخبار ±15-20د · فحص AutoTrading · watchdog يُحيي 21 محرّكاً خلال 60ث     │
└──────────────────────────────────────┬───────────────────────────────────────┘
                                       ▼
┌──────────────────────────── طبقة المراقبة (Observe) ─────────────────────────┐
│ ARENA :8870 (لعبة: أبطال/مخاطر ICA/استخبارات/تطوّر/خريطة)                     │
│ truth_tracker (10د): لوحة الحقيقة — الحكم النهائي                             │
│ evolution_log · straddle_journal · سرب PlutoBrain (46 وكيل تحليل كود) :8099   │
└───────────────────────────────────────────────────────────────────────────────┘
```

## 2) الحلقات الذاتية (كل شيء بلا تدخّل)
| الحلقة | الدورية | تُطوّر |
|---|---|---|
| accuracy_updater | 10 دقائق | أوزان الـ31 مؤشراً لكل عملة (دقّة مقيسة) |
| evolution_director (الأوضاع) | 30 دقيقة | تعطيل/تعزيز استراتيجيات الدخول بالنتائج الحيّة |
| secure_learner | ساعة | سرعة تأمين الربح لكل عملة |
| _reoptimize_loop | 3 ساعات | الجينات (الخاسر أولاً: شبكة واسعة + تهجين) |
| evolution_director (الجلسات) | يومياً | إعادة تحقّق متخصّصي الجلسات 50k+سبريد حقيقي |
| evolution_director (الاكتشاف) | يومياً | رموز جديدة تدخل تلقائياً إذا أثبتت |
| straddle self-eval | 10 دقائق | معايير الكمّاشة (متغيّر واحد/دورة) + طلب مساعدة |
| risk_manager | دقيقة | الأحجام والتكديس حسب الخطر |
| watchdog | دقيقة | البقاء (إحياء الميّت) |

## 3) الفجوات (⚠ بالأولوية) وخطة التعديل
1. **⚠⚠ الربحية الحيّة غير مثبتة (الفجوة الجوهرية).** OOS ≠ حيّ. **الخطة:** أسبوع قياس نظيف عبر لوحة الحقيقة لكل مصدر (multi_trader أوضاع، straddle، unified) → ما يثبت يتوسّع، وما يفشل يُبنّح آلياً (الآلية موجودة الآن — تحتاج وقتاً وبيانات فقط).
2. **⚠⚠ نزيف خارج الحوكمة:** اليدوي + EAs قديمة (20260600، 20250422، stacker) تخسر أكثر من البوتات ولا يحكمها شيء. **الخطة:** إيقاف/عزل الـEAs القديمة (قرارك)، وأدنى حدّ: تقرير حقيقة يفصلها (موجود).
3. **⚠ حوكمة غير موحّدة:** btc_live/coordinator/unified لا يقرؤون directive/risk/mode_weights (multi_trader فقط). **الخطة:** توحيد — الجميع يقرأ risk_register + mode_weights (تعديل صغير لكل منفّذ).
4. **⚠ ذهب مزدوج:** unified (كل الجلسات، جين أكاديمية) + multi_trader (جلسات مُتحقّقة) على نفس الرمز. **الخطة:** إمّا قصر unified على ساعات NY أو إخضاعه للموجّه — قرار بعد أسبوع القياس.
5. **⚠ لا سقف ارتباط محفظي:** USDJPY+JP225+ذهب قد يتحرّكون معاً. **الإضافة المقترحة:** حدّ تعرّض كتلي (كتلة آسيا/الدولار) في risk_manager (R11).
6. **تضخّم مؤشرات:** 31 مؤشراً أغلبها ~50-53% — كلفة حساب بلا حافة. **الحذف المقترح:** تقليم آلي — أي مؤشر وزنه المتعلّم <0.1 لكل العملات لمدة أسبوع يُحذف من الحساب (يبقى بالسجل).
7. **قرارات التطوّر غير ظاهرة في ARENA:** mode bench/discovery تُسجَّل في اللوج فقط. **الإضافة:** بطاقة "قرارات المدير" في تبويب التطوّر.
8. **لا ملخّص يومي:** **الإضافة:** كاتب تقرير ليلي (digest) من اللوحات → ملف/لوحة "وش صار بغيابك".
9. **نقطة فشل وحيدة:** منصّة MT5/AutoTrading — الحارس ينبّه ولا يصلح (حدّ منصّة).
10. **توسيع الصيّاد:** رابح — يحتاج قاعدة ترقية (بعد 10 محاولات صافي≥+$3 → لوت ×2 تدريجياً).

## 4) خارطة الطريق المحدّثة
اكتشاف ✅ → بوّابة OOS+سبريد حقيقي ✅ → مصنع جينات+جلسات ✅ → حوكمة (محلّل+مخاطر+أوضاع) ✅ → تطوّر ذاتي كامل ✅ → **إثبات حيّ أسبوع (نحن هنا)** → توحيد الحوكمة + سقف الارتباط → ترقية الرابحين فقط → فلوس حقيقية (بإذنك فقط).
